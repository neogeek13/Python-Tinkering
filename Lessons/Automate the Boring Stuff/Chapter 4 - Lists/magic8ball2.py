import random

messages = ['It is certain',
    'It is decidedly so',
    'Yes',
    'Reply hazy try again',
    'Ask again later',
    'Concentrate and ask again',
    'My reply is no',
    'Outlook not so good',
    'Very doubtful']

# r = random.randint(1,9)
# fortune = getAnswer(r)
# print(fortune)

# print(messages[random.randint(1,9)])

print(messages[random.randint(0,len(messages) - 1)])

